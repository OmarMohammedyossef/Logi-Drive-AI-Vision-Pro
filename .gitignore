# NOTE: site/ is intentionally NOT ignored. The built site is committed so that
# Vercel has nothing to build — see "Two ways to deploy" in README.md.
.cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Obsidian per-machine UI state
.obsidian/workspace.json

# OS noise
.DS_Store
Thumbs.db
